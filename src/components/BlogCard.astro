---
interface Props {
  titol: string;
  slug: string;
  dataPublicacio: string;
  extracte?: string;
  imatgePrincipal?: string;
}
const { titol, slug, dataPublicacio, extracte, imatgePrincipal } = Astro.props;
const date = new Date(dataPublicacio).toLocaleDateString('ca-ES', { day: 'numeric', month: 'long', year: 'numeric' });
---
<article class="blog-card reveal">
  <a href={`/blog/${slug}`}>
    {imatgePrincipal && (
      <div class="card-image">
        <img src={imatgePrincipal} alt={titol} loading="lazy" />
        <div class="card-overlay" aria-hidden="true">
          <span class="overlay-title">{titol}</span>
        </div>
      </div>
    )}
    <div class="card-body">
      <time class="card-date">{date}</time>
      <h3>{titol}</h3>
      {extracte && <p>{extracte}</p>}
    </div>
  </a>
</article>

<style>
  .blog-card { border: 1px solid var(--color-border); transition: border-color 0.2s; }
  .blog-card:hover { border-color: var(--color-black); }
  .blog-card a { display: block; text-decoration: none; }

  .card-image {
    aspect-ratio: 16/9;
    overflow: hidden;
    position: relative;
  }
  .card-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.6s ease;
    display: block;
  }
  .blog-card:hover .card-image img { transform: scale(1.04); }

  /* Overlay */
  .card-overlay {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.45);
    display: flex;
    align-items: flex-end;
    padding: 1.25rem;
    opacity: 0;
    transform: translateY(100%);
    transition: opacity 0.4s ease, transform 0.4s ease;
  }
  .blog-card:hover .card-overlay {
    opacity: 1;
    transform: translateY(0);
  }
  .overlay-title {
    color: var(--color-white);
    font-size: 1rem;
    font-weight: 600;
    line-height: 1.3;
  }

  @media (prefers-reduced-motion: reduce) {
    .card-image img { transition: none; }
    .card-overlay { transition: none; }
  }

  .card-body { padding: 1.25rem; }
  .card-date {
    font-size: 0.75rem;
    color: var(--color-gray-light);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    display: block;
    margin-bottom: 0.5rem;
  }
  .card-body h3 { font-size: 1rem; font-weight: 600; margin-bottom: 0.5rem; }
  .card-body p { font-size: 0.875rem; color: var(--color-gray-dark); }
</style>
