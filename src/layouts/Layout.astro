---
import Nav from '../components/Nav.astro';
import Footer from '../components/Footer.astro';

interface Props {
  title: string;
  description?: string;
  image?: string;
}

const { title, description = "Espai d'art al cor de Girona", image } = Astro.props;
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
---

<!doctype html>
<html lang="ca">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalURL} />
    <title>{title} — Espai Barri Vell</title>
    {image && <meta property="og:image" content={image} />}
    <meta property="og:title" content={`${title} — Espai Barri Vell`} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <link rel="preload" href="/fonts/ChampagneLimousines-Bold.woff2" as="font" type="font/woff2" crossorigin />
    <link rel="preload" href="/fonts/FuturaLTPro-Book.woff2" as="font" type="font/woff2" crossorigin />
    <link rel="preload" href="/fonts/FuturaLTPro-Heavy.woff2" as="font" type="font/woff2" crossorigin />
  </head>
  <body>
    <Nav />
    <main>
      <slot />
    </main>
    <Footer />
  </body>
</html>

<style is:global>
  /* ── Fonts corporatives (GuideEBV), servides en local ── */
  @font-face {
    font-family: 'Champagne & Limousines';
    src: url('/fonts/ChampagneLimousines-Regular.woff2') format('woff2');
    font-weight: 400;
    font-style: normal;
    font-display: swap;
  }
  @font-face {
    font-family: 'Champagne & Limousines';
    src: url('/fonts/ChampagneLimousines-Bold.woff2') format('woff2');
    font-weight: 700;
    font-style: normal;
    font-display: swap;
  }
  @font-face {
    font-family: 'Futura LT Pro';
    src: url('/fonts/FuturaLTPro-Book.woff2') format('woff2');
    font-weight: 400;
    font-style: normal;
    font-display: swap;
  }
  @font-face {
    font-family: 'Futura LT Pro';
    src: url('/fonts/FuturaLTPro-BookOblique.woff2') format('woff2');
    font-weight: 400;
    font-style: italic;
    font-display: swap;
  }
  @font-face {
    font-family: 'Futura LT Pro';
    src: url('/fonts/FuturaLTPro-Medium.woff2') format('woff2');
    font-weight: 500;
    font-style: normal;
    font-display: swap;
  }
  @font-face {
    font-family: 'Futura LT Pro';
    src: url('/fonts/FuturaLTPro-Heavy.woff2') format('woff2');
    font-weight: 800;
    font-style: normal;
    font-display: swap;
  }

  :root {
    /* ── Paleta corporativa (GuideEBV) ────────────── */
    --color-ink:   #161616;
    --color-paper: #FFFFFF;
    --color-cream: #F6F3EC;
    --color-black: var(--color-ink);
    --color-white: var(--color-paper);
    --color-gray-dark: #606060;
    --color-gray-light: #a3a3a3;
    --color-border: rgba(22, 22, 22, 0.10);

    --color-brand-blue:   #5EBCEC; /* text negre */
    --color-brand-orange: #E36F00; /* text negre */
    --color-brand-red:    #C82A3E; /* text blanc */
    --color-brand-teal:   #3F7E7B; /* text blanc */
    --color-brand-yellow: #F0C030; /* text negre */
    --color-accent:       var(--color-brand-teal);

    /* ── Tipografies corporatives (GuideEBV) ──────── */
    /* H1: Champagne & Limousines Bold · H2/H3: Futura LT Pro Heavy · Text: Futura LT Pro Book */
    --font-heading: 'Champagne & Limousines', 'Futura', 'Century Gothic', sans-serif;
    --font-sans: 'Futura LT Pro', 'Futura', 'Century Gothic', sans-serif;
    --font-serif: var(--font-heading);
    --container-max: 1200px;
    --spacing-xs: 0.5rem;
    --spacing-sm: 1rem;
    --spacing-md: 2rem;
    --spacing-lg: 4rem;
    --spacing-xl: 6rem;
  }

  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  html { font-size: 16px; }

  body {
    font-family: var(--font-sans);
    font-size: 0.875rem; /* GuideEBV: text 12px; 14px per llegibilitat web */
    color: var(--color-ink);
    background: var(--color-paper);
    line-height: 1.6;
  }

  a { color: inherit; text-decoration: none; }
  a:hover { text-decoration: underline; }

  img { max-width: 100%; height: auto; display: block; }

  .container {
    width: 100%;
    max-width: var(--container-max);
    margin: 0 auto;
    padding: 0 var(--spacing-md);
  }

  /* Jerarquia tipogràfica GuideEBV: H1 32px bold · H2 24px heavy · H3 20px heavy */
  h1 { font-family: var(--font-heading); font-weight: 700; font-size: clamp(1.75rem, 3vw, 2rem); line-height: 1.2; }
  h2 { font-family: var(--font-sans); font-weight: 800; font-size: 1.5rem; line-height: 1.2; }
  h3 { font-family: var(--font-sans); font-weight: 800; font-size: 1.25rem; line-height: 1.2; }
  h4 { font-family: var(--font-sans); font-weight: 800; line-height: 1.2; }

  .section { padding: var(--spacing-xl) 0; }
  .section-title {
    font-family: var(--font-heading);
    font-size: 1.5rem;
    font-weight: 700;
    margin-bottom: var(--spacing-md);
    padding-bottom: var(--spacing-sm);
    border-bottom: 1px solid var(--color-border);
  }

  .btn {
    display: inline-block;
    padding: 0.75rem 1.75rem;
    border: 1px solid var(--color-black);
    background: var(--color-white);
    color: var(--color-black);
    font-family: var(--font-sans);
    font-size: 0.875rem;
    font-weight: 500;
    cursor: pointer;
    transition: background 0.2s ease, color 0.2s ease;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    will-change: transform;
  }
  .btn:hover { background: var(--color-ink); color: var(--color-paper); text-decoration: none; }
  .btn-primary { background: var(--color-ink); color: var(--color-paper); }
  .btn-primary:hover { background: var(--color-gray-dark); }

  /* ─── Etiquetes (GuideEBV: categories, filtratge botiga, blog) ── */
  .tag {
    display: inline-block;
    padding: 0.2rem 0.55rem;
    font-family: var(--font-sans);
    font-size: 0.65rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    background: var(--color-ink);
    color: var(--color-paper);
  }
  .tag-red    { background: var(--color-brand-red);    color: var(--color-paper); }
  .tag-teal   { background: var(--color-brand-teal);   color: var(--color-paper); }
  .tag-blue   { background: var(--color-brand-blue);   color: var(--color-ink); }
  .tag-yellow { background: var(--color-brand-yellow); color: var(--color-ink); }
  .tag-orange { background: var(--color-brand-orange); color: var(--color-ink); }
  .tag-outline { background: var(--color-paper); color: var(--color-ink); border: 1px solid var(--color-ink); }

  .grid-2 { display: grid; grid-template-columns: repeat(2, 1fr); gap: var(--spacing-md); }
  .grid-3 { display: grid; grid-template-columns: repeat(3, 1fr); gap: var(--spacing-md); }
  .grid-4 { display: grid; grid-template-columns: repeat(4, 1fr); gap: var(--spacing-md); }

  @media (max-width: 768px) {
    .grid-2, .grid-3, .grid-4 { grid-template-columns: 1fr; }
    .container { padding: 0 var(--spacing-sm); }
  }
  @media (min-width: 769px) and (max-width: 1024px) {
    .grid-3, .grid-4 { grid-template-columns: repeat(2, 1fr); }
  }

  /* ─── Reveal animations ──────────────────────────────────── */
  .reveal {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.7s ease, transform 0.7s ease;
  }
  .reveal.is-visible {
    opacity: 1;
    transform: translateY(0);
  }

  .reveal-stagger .reveal-item {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.7s ease, transform 0.7s ease;
  }
  .reveal-stagger.is-visible .reveal-item {
    opacity: 1;
    transform: translateY(0);
  }

  @media (prefers-reduced-motion: reduce) {
    .reveal,
    .reveal-stagger .reveal-item {
      opacity: 1 !important;
      transform: none !important;
      transition: none !important;
    }
  }

  /* ─── Image wipe reveal ──────────────────────────────────── */
  .img-reveal {
    position: relative;
    overflow: hidden;
    display: block;
  }
  .img-reveal::after {
    content: '';
    position: absolute;
    inset: 0;
    background: var(--color-black);
    transform: translateX(0);
    transition: transform 0.9s cubic-bezier(0.77, 0, 0.175, 1);
  }
  .img-reveal.is-visible::after {
    transform: translateX(100%);
  }

  @media (prefers-reduced-motion: reduce) {
    .img-reveal::after { display: none; }
  }

  /* ─── Page transition ────────────────────────────────────── */
  main {
    opacity: 0;
    animation: page-fade-in 0.4s ease forwards;
  }

  @keyframes page-fade-in {
    to { opacity: 1; }
  }

  @media (prefers-reduced-motion: reduce) {
    main { opacity: 1; animation: none; }
  }
</style>

<script>
  /* ─── Intersection Observer: reveal + img-reveal ─────────── */
  (function () {
    if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;

    const revealObserver = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add('is-visible');
            revealObserver.unobserve(entry.target);
          }
        });
      },
      { threshold: 0.15 }
    );

    document.querySelectorAll('.reveal').forEach((el) => revealObserver.observe(el));
    document.querySelectorAll('.img-reveal').forEach((el) => revealObserver.observe(el));

    const staggerObserver = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            const items = entry.target.querySelectorAll<HTMLElement>('.reveal-item');
            items.forEach((item, i) => {
              item.style.transitionDelay = `${i * 0.12}s`;
            });
            entry.target.classList.add('is-visible');
            staggerObserver.unobserve(entry.target);
          }
        });
      },
      { threshold: 0.1 }
    );

    document.querySelectorAll('.reveal-stagger').forEach((el) => staggerObserver.observe(el));
  })();

  /* ─── Page transition: fade-out on internal link click ───── */
  (function () {
    if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;

    document.addEventListener('click', (e) => {
      const anchor = (e.target as Element)?.closest('a');
      if (!anchor) return;
      const href = anchor.getAttribute('href');
      if (!href) return;
      if (href.startsWith('#') || href.startsWith('http') || href.startsWith('mailto')) return;
      if (anchor.target === '_blank') return;

      e.preventDefault();
      const main = document.querySelector('main') as HTMLElement | null;
      if (main) {
        main.style.transition = 'opacity 0.2s ease';
        main.style.opacity = '0';
        setTimeout(() => { window.location.href = href; }, 200);
      } else {
        window.location.href = href;
      }
    });
  })();

  /* ─── Magnetic buttons ───────────────────────────────────── */
  (function () {
    if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;
    if (!window.matchMedia('(hover: hover) and (pointer: fine)').matches) return;

    document.querySelectorAll<HTMLElement>('.btn').forEach((btn) => {
      btn.addEventListener('mousemove', (e) => {
        const rect = btn.getBoundingClientRect();
        const x = (e.clientX - rect.left - rect.width / 2) * 0.22;
        const y = (e.clientY - rect.top - rect.height / 2) * 0.22;
        btn.style.transform = `translate(${x}px, ${y}px)`;
        btn.style.transition = 'background 0.2s ease, color 0.2s ease, transform 0.08s ease';
      });
      btn.addEventListener('mouseleave', () => {
        btn.style.transform = 'translate(0, 0)';
        btn.style.transition = 'background 0.2s ease, color 0.2s ease, transform 0.5s cubic-bezier(0.34, 1.56, 0.64, 1)';
      });
    });
  })();

  /* ─── Card tilt (efecte 3D subtil) ───────────────────────── */
  (function () {
    if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;
    if (!window.matchMedia('(hover: hover) and (pointer: fine)').matches) return;

    const cards = document.querySelectorAll<HTMLElement>(
      '.exhibition-card, .artist-card, .artwork-card, .blog-card'
    );

    cards.forEach((card) => {
      card.addEventListener('mousemove', (e) => {
        if (!card.classList.contains('is-visible')) return;
        const rect = card.getBoundingClientRect();
        const x = (e.clientX - rect.left) / rect.width - 0.5;
        const y = (e.clientY - rect.top) / rect.height - 0.5;
        card.style.transform = `perspective(800px) rotateY(${x * 5}deg) rotateX(${-y * 5}deg) translateZ(6px)`;
        card.style.transition = 'transform 0.1s ease, border-color 0.2s';
      });
      card.addEventListener('mouseleave', () => {
        card.style.transform = 'perspective(800px) rotateY(0deg) rotateX(0deg) translateZ(0px)';
        card.style.transition = 'transform 0.5s cubic-bezier(0.34, 1.56, 0.64, 1), border-color 0.2s';
        setTimeout(() => {
          if (!card.matches(':hover')) {
            card.style.transform = '';
            card.style.transition = '';
          }
        }, 520);
      });
    });
  })();
</script>
